# --- Fine-tuning (LoRA / QLoRA) dependencies ---
# Device-adaptive: runs LoRA on CPU, or 4-bit QLoRA when a CUDA GPU is present.
#
# Install torch matching your hardware FIRST (https://pytorch.org/get-started/locally/):
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pick the CUDA build from the pytorch site
# Then:  pip install -r requirements.txt

transformers>=4.43.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.33.0

# GPU ONLY (4-bit QLoRA). Skip on CPU — it requires CUDA and will fail to install/run.
# Install manually on a GPU box:  pip install bitsandbytes>=0.43.0

# Reused by build_dataset.py (also in the parent pipeline requirements)
ollama>=0.3.0
pypdf>=4.0.0
pdfplumber>=0.11.0
pandas>=2.0.0
openpyxl>=3.1.0
pytesseract>=0.3.10
Pillow>=10.0.0
tqdm>=4.66.0
