"""Tool Registry Framework.

Auto-discovers tools from the agents-tools-library, parses their README.md to
understand capabilities, builds JSON schemas, and exposes them to agents and the
workflow engine as OpenClaw-compatible tools — with zero code changes per tool.
"""
